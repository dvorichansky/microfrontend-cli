const { dependencies } = require('./package.json');

module.exports = {
    name: '<%= name %>',
<% if (type === 'microfrontend') { -%>
    exposes: {
        './App': './src/App',
    },
    filename: 'remoteEntry.js',
<% } -%>
    remotes: {
        // shared: 'shared@http://localhost:4000/remoteEntry.js',
    },
    shared: {
        ...dependencies,
        react: {
            singleton: true,
            requiredVersion: dependencies['react'],
        },
        'react-dom': {
            singleton: true,
            requiredVersion: dependencies['react-dom'],
        },
    },
};
